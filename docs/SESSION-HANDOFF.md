# Session Handoff: Epic #51 COMPLETE + #75 SHIPPED + #76 installments smoke VERIFIED & CLOSED — backlog fully clear (no open issues)

Last updated: 2026-06-11. Repo: `main` @ `f5bb9de` (Zod 4.4.3 / TypeScript 6 / Vitest 4), single clean worktree `~/repos/pipedrive-mcp-server`. Build green (`tsc`), **1692 tests green**, CI green Node 20/22. No agent worktrees, branches, or locks outstanding. **No open issues** — #76 (installments smoke test, P3) was verified live against a Pipedrive Growth trial and CLOSED 2026-06-11 (`status:done`). The smoke harness (`scripts/smoke-installments.ts`, `npm run smoke:installments`; runbook `docs/installments-smoke-test.md`) remains for future re-verification. NOTE: the harness commit `bba65ac` is on local `main` but may still be UNPUSHED — check `git status` / `git log origin/main..main`.

**The v1→v2 API migration completeness epic (#51) is fully shipped and closed, and #76 (the last open issue) is now verified and closed too — the backlog is empty.** #75 (contract-harness coverage for Products + the #70 config-field writes) SHIPPED & CLOSED this session via PR **#77** → `main @ f5bb9de`. #76 (installments smoke) was verified live against a Pipedrive Growth trial on 2026-06-11 (all four tools: add/update/delete round-trip + single and comma-joined `deal_ids` list returning both deals' installments; criterion 3 not-entitled mapping already confirmed against a below-Growth sandbox) and closed as completed with no code change. This handoff carries forward the durable gotchas.

## Done this session (do NOT redo)

- **#76 smoke harness BUILT + VERIFIED LIVE + #76 CLOSED.** Added `scripts/smoke-installments.ts` (run via `npm run smoke:installments`) driving all four deal-installments tools through the real `handleCallTool` path (Zod -> handler -> client): it creates a throwaway product + two deals (each gets a one-time product, the installments precondition), runs the add/update/delete round-trip plus `list` with single and multiple `deal_ids` (asserting BOTH deals' installments come back, which proves the comma-join resolves server-side rather than collapsing repeated keys), then tears everything down. Separate `--mode=not-entitled` check confirms a non-Growth token yields a clean mapped error envelope, not a crash. Runbook + Growth+ token-acquisition guide in `docs/installments-smoke-test.md`. Build/lint/tests unaffected: the script lives outside `src/`, so it is excluded from `tsc` (`include: ["src/**/*"]`), `eslint src/`, and the vitest glob (`tests/**/*.test.ts`). Run live against a Pipedrive Growth trial on 2026-06-11: all four tools passed (the comma-joined `deal_ids` list returned BOTH deals' installments, each carrying its own `deal_id`, proving server-side resolution), no spec discrepancy, so #76 was closed as completed. An earlier run against a below-Growth dev sandbox confirmed criterion 3 (clean `PERMISSION_DENIED` mapping) and established the durable fact that **Pipedrive developer sandboxes are NOT Growth+ by default** (installments 403 at the API + show a `GROWTH` upsell badge in the UI). The harness now reports such accounts as 🔒 BLOCKED rather than a misleading failure.
- **#75 contract-harness coverage SHIPPED & CLOSED** via PR **#77** (squash) → `main @ f5bb9de`. **18 new contract tests** wiring the surface deferred at ship time into `tests/contract/`: Products params (`getProducts`/`searchProducts`) + body (`addProduct`/`updateProduct`), and the #70 config-field writes — field CRUD bodies (deal/person/org/product, guarding the v2 `field_name`/`field_type` rename), the option bulk verbs (`updateDealFieldOptions` PATCH array body, `deleteDealFieldOptions` body-bearing DELETE array body), and pipeline/stage bodies (`is_deal_probability_enabled`; `is_deal_rot_enabled`/`days_to_rotten`). Two harness extensions were required (see gotchas): `assertArrayBodyConformsToSpec()` for top-level JSON-array bodies, and `allOf`-merge in `requestBodyProps`/`requestBodyPropType` (Product write schemas are `allOf`-composed). False-green proven on 4 independent shape reverts. Code-reviewed (extra-high recall): no correctness bugs; fixed two field-test comments that overclaimed a `description`-leak guard the body-shape check can't provide.
- **Epic #51 CLOSED.** All phases done: #42–#50 + #60 and Phase 3b #67 (PR #72) / #68 (PR #73) / #69 (PR #71) / #70 (PR #74); #75 (PR #77) cleared the deferred contract-test debt. Checklist ticked, completion comment posted.

### Prior sessions (context only — all shipped & closed)

- **#70 — Config writes** → PR #74 (`9277179`). 26 tools (→155 total): pipeline/stage + deal/person/org/product field CRUD + bulk option PATCH/DELETE (deferred-from-#50 surface). `src/client.ts` widened to accept top-level array bodies + optional `delete` body. v2 renames enforced via `.strict()` schemas with REJECT tests; `field_code` = server-generated 40-char hash. **Security P1 fixed:** `FieldCodeSchema` switched from a `/`-only blocklist to an allowlist `/^[A-Za-z0-9_-]+$/` because `field_code` is interpolated into paths and `new URL()` normalizes `\`/`..`/`?`/`#` (see Path-interpolation gotcha below).
- **#69 — Cross-entity followers + multipart media** → PR #71 (`4dbf749`). deal/person/org follower CRUD+changelog (#69 OWNS deal followers); `get_person_picture` (read-only); `postMultipart`/`putMultipart`; product-image upload (POST) + update (PUT), hybrid `file_path`|`base64_data`.
- **#67 — Deal sub-resources** → PR #72 (`f89aca0`). discounts (`discount_id` UUID, no list pagination); installments collection-level `/deals/installments` with comma-joined `deal_ids`; convert-to-lead gated + empty `{}` POST + status-poll.
- **#68 — Project sub-entities** → PR #73 (`ab51d5c`). tasks/boards/phases/templates/project-fields/archived/permittedUsers/changelog; task `done`/`milestone` write as int `0|1` (GET returns bool) via `z.preprocess`; `/phases` list requires `board_id`; boards/phases have no pagination; `permittedUsers` returns flat `integer[]`.

## Open follow-ups (now tracked on the board)

- **(none open).** #76 (installments smoke test, P3) was the last follow-up; it was verified live against a Growth trial and CLOSED 2026-06-11 (`status:done`, reason completed). The reusable harness is `scripts/smoke-installments.ts` (`PIPEDRIVE_API_KEY=<growth-token> PIPEDRIVE_ENABLE_DESTRUCTIVE=true npm run smoke:installments -- --confirm-sandbox`); see `docs/installments-smoke-test.md`. **The board is empty — no open issues.**

## Known gotchas (carried — still apply to any future work)

- **Path interpolation needs an allowlist.** Any value interpolated into a request path (string `field_code`/keys; integer IDs via `z.number().int().positive()` are safe) must use a strict allowlist regex, NOT a `/`-only blocklist — `new URL()` normalizes `\`, `..`, `?`, `#`. (See #70 P1 above.)
- **MERGE-GATE GOTCHA.** Do NOT use `gh pr merge <n> --squash --delete-branch` (its local-branch delete fails because the **worktree** holds the branch, and it skips the remote delete). Use **`gh pr merge <n> --squash` WITHOUT `--delete-branch`**, then manual cleanup from the MAIN repo root: `git worktree remove <path> --force` → `git branch -D <branch>` → `git push origin --delete <branch>` → `git worktree prune` → `rm -r .claude/menehune/locks/<n>.lock` → `git checkout main && git pull --ff-only`. Verify with `gh pr view <n> --json state,mergedAt`, `git worktree list`, `git ls-remote --heads origin <branch>` (don't trust exit codes).
- **MERGE-DIVERGENCE GOTCHA (new, #70).** If you rebase an agent branch onto a LOCAL `main` that is ahead of `origin/main` (e.g. an unpushed handoff-doc commit), those unpushed commits ride into the pushed PR branch as ancestors and the GitHub squash folds them into the merge commit. After merge, `git pull --ff-only` then FAILS ("Not possible to fast-forward") because local/origin diverged at the shared base. Fix: confirm the unpushed change is contained in the squash (`git diff <local-commit> origin/main -- <file>` is empty), then `git reset --hard origin/main`.
- **Adding a param means editing THREE places:** the Zod schema, the hand-written `inputSchema` JSON literal (NOT generated from Zod), and the handler's query/body/path forward line.
- **`git checkout <ref> -- src/` does NOT delete files absent from `<ref>`** — for a complete revert-proof, `rm` new files explicitly, then `git checkout origin/main -- <modified existing files>`.
- **Verify LLM-facing tool-description text against the actual Zod INPUT schema, not the API query key.** (#68 R2 caught a description saying `project_id` when the input field is `id`.)
- **Auth reality (supersedes CLAUDE.md prose):** v2 uses the **`x-api-token` HEADER** (`src/client.ts` `applyAuth`), NOT the `api_token` query param (v1-only).
- **`.env` is auto-loaded by anything importing `src/index.ts`** (it does `import "dotenv/config"`), so dev scripts/tests that import the server inherit the repo's gitignored `PIPEDRIVE_API_KEY` as a FALLBACK. An explicit shell `PIPEDRIVE_API_KEY=…` overrides it (dotenv never overwrites an already-set var). The #76 smoke harness relies on this but guards full mode (which CREATES/DELETES real records) behind a required `--confirm-sandbox` flag plus a masked token-tail print, so it cannot silently run writes against whatever account `.env` points at. Re-apply that pattern for any future live-API script.
- **Lint IS a CI gate** (`.github/workflows/ci.yml` runs `npm run lint` = `eslint src/`; test files NOT linted). Verify with `npx eslint src/`.
- **Zod 4.4.3 on main** (NOT Zod 3). Idioms: `z.email()`/`z.uuid()`, `z.record(z.string(), z.unknown())`, `.extend(...shape)` (not `.merge`), `error.issues` (not `.errors`). Zod `.refine({path})` wants a MUTABLE `PropertyKey[]` — do NOT `as const` the options object (a readonly `path` fails `tsc`).
- **`createListSummary` naively appends "s"** for plurals — assert summaries with `toContain`, not exact `toBe`.
- **False-green hazard:** `tsconfig` excludes `**/*.test.ts` (tests never type-checked) and per-entity tests call handlers DIRECTLY (bypass the dispatcher's Zod). Real guards = UNIT schema tests (`Schema.parse`, `.strict()` REJECT tests) + integration tests asserting the wire shape (`JSON.parse(options.body)` / the URL) + the `tests/contract/` OpenAPI harness.
- **Contract harness handles array bodies + composed schemas (#75).** `tests/contract/helpers/openapiContract.ts` now exposes `assertArrayBodyConformsToSpec(opId, body)` for ops whose v2 request body is a top-level JSON array (the field-option bulk verbs); the object-only `assertBodyConformsToSpec` rejects non-objects by design. `requestBodyProps`/`requestBodyPropType` merge top-level `allOf` via `collectProperties`, so `allOf`-composed bodies (`addProduct`/`updateProduct`) resolve their keys instead of returning `[]`. CAVEAT: the body-shape check only sees the keys a handler actually emits, and contract tests bypass Zod — so it CANNOT prove a narrower schema (e.g. person/product field bodies excluding `description`); that exclusion is the Zod `.strict()` schema's job. The array assertion is shape-only: no `minItems`, so an empty array passes, and it skips item key-checks when the spec `items` declares no `properties`.
- **Test layout:** unit schema tests in `tests/unit/schemas/` (3-level import), integration handler tests in `tests/integration/tools/`, helpers at `tests/helpers/`. **`setupValidEnv()` sets `PIPEDRIVE_ENABLE_DESTRUCTIVE='true'` in `beforeEach`** — "enabled" delete tests work by default; "guard blocks" tests must `delete process.env.PIPEDRIVE_ENABLE_DESTRUCTIVE` explicitly.
- **v1 sunset:** working horizon 2026-07-31 (partner-sourced; Pipedrive's first-party 2025-12-31 covers only selected endpoints, NOT notes/mail/users/leads CRUD). See `docs/v1-only-capabilities.md`. Re-verify before any v1-removal work.
