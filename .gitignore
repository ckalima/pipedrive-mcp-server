# Dependencies
node_modules/

# Build output
dist/

# MCPB bundle: manifest.json is generated + tracked (see npm run gen:docs);
# the compiled server/ tree and packed .mcpb are rebuilt by npm run bundle:mcpb.
bundle/server/
*.mcpb
!bundle/manifest.json

# Environment
.env
.env.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Test coverage
coverage/

# Claude Code / Menehune local state (machine-specific, not shared)
.claude/settings.local.json
.claude/prioritize-cache.md
.claude/menehune/
.claude/worktrees/

# Private docs lane — never commit to this public repo. Competitive strategy
# and session handoffs (may contain account IDs / PII) live here; back them up
# in ckalima/pipedrive-mcp-internal. Engineering plans, brainstorms, and review
# findings ARE public and live under docs/ (tracked). See "Docs" in CLAUDE.md.
docs/private/
docs/SESSION-HANDOFF.md

# Menehune backlog config (machine-specific; regenerated by /backlog:setup)
.claude/backlog-reference.md
